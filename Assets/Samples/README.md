# Samples Folder

To enable the optional samples, copy the contents from `Samples~` into this folder.

The samples are maintained in the `Samples~` folder at the root of the package, which is the standard location for optional samples in Unity packages.

